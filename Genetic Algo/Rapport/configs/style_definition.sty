
% redéfinie les headerule
\renewcommand{\headrule}{
\vspace{-8pt}\hrulefill
\raisebox{-2.1pt}{\quad\decofourleft\decotwo\decofourright\quad}\hrulefill}


%redéfini les headers
\pagestyle{fancy}
\addtolength{\headwidth}{\marginparsep}
\renewcommand{\headrulewidth}{2pt}
\renewcommand{\chaptermark}[1]{\markboth{#1}{#1}}
\fancyhead[R]{\doctitle{}}
\fancyhead[L]{\textit{\chaptername\ \thechapter\ --\ \leftmark}}

% définie la distance entre chaque ligne
\onehalfspacing

% définie la taille des titres de sections
\sectionfont{\fontsize{18pt}{21pt}\selectfont}

% définie la couleur des hyperlien
\definecolor{VDBlue}{HTML}{4a004a}

% enlève le compte des pages
\pagenumbering{gobble}

% change la forme des footer
\fancyfoot[C]{}
% page x sur y
\fancyfoot[R]{Page \thepage \hspace{1pt} sur \pageref{LastPage}}

% redéfinie le style plain.
\fancypagestyle{plain}{%
\renewcommand{\headrule}{%
\vspace{-8pt}}
\fancyhf{}
\ifthenelse{\thepage > 0}{
    \fancyfoot[R]{Page \thepage \hspace{1pt} sur \pageref{LastPage}}
}{}
}

\renewcommand{\chaptermark}[1]{%
  \markboth{#1}{}}

% définie le style de latex pour le lstlisting
\lstdefinestyle{latex}
{
  language=[LaTeX]{TeX},
  texcsstyle=*\color{blue},
  basicstyle=\ttfamily,
  frame=single,
}